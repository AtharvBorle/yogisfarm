const fs = require('fs'); const html = fs.readFileSync('C:/Users/Admin/Downloads/yogisfarm_migration/yogisfarm/figma_home_html.txt', 'utf8'); const regex = /src="data:image\/[^;]+;base64,[^"]+"/g; let match; let count = 0; let results = []; while ((match = regex.exec(html)) !== null) { count++; results.push({ count, index: match.index }); } function findNearest(text) { const idx = html.indexOf(text); const before = results.filter(r => r.index < idx); return before.length > 0 ? before[before.length - 1].count : null; } console.log('Chemical:', findNearest('Chemical Free')); console.log('Cruelty:', findNearest('Cruelty Free')); console.log('Vegetarian:', findNearest('Indian & Vegetarian')); console.log('Fresh:', findNearest('Always Fresh')); console.log('Natures Basket:', findNearest('Nature\\\'s Basket')); console.log('Swiggy:', findNearest('Swiggy Instamart')); console.log('Jio:', findNearest('Jio Mart')); console.log('Nilgiris:', findNearest('Nilgiris'));
